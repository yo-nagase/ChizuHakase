# めざせ! ちずはかせ

子ども向けの都道府県学習アプリ。日本地図をタップして県を当てるクイズを軸に、
「全国マイマップ」と「とくさんひんカード」で継続動機をつくる。

---

## 1. プロダクト前提

| 項目 | 内容 |
| --- | --- |
| 名前 | **めざせ! ちずはかせ**。ホーム画面は「ちずはかせ」(9 文字はアイコン下で切れる)。英語ストア表記は *Chizu Hakase: Japan Map Quiz for Kids*(UI は日本語のみなので、英訳ブランドで英語アプリに見せない) |
| 内部名 | `ChizuHakase` / `com.wakuwaku.chizuhakase`。未公開のうちに新名称へ揃えた(bundle ID は一度公開すると永久に固定)。公開後の改名は不可 |
| プラットフォーム | iOS 17.0+ / iPadOS 17.0+ |
| 言語・UI | Swift 6, SwiftUI |
| 主対象 | 5〜9歳(ひらがなが読めるかどうか怪しい層を含む) |
| 課金 | **なし**。全機能を無料で開放。広告なし |
| 通信 | **なし**。完全オフライン動作 |
| 表示言語 | 日本語のみ(UI文言はすべてひらがな中心) |

### 絶対に守る制約

これらは App Store Kids カテゴリ(Guideline 1.3 / 5.1.4)への適合条件であり、
実装判断で覆さないこと。迷ったら実装せずに確認を求める。

- **広告 SDK を入れない。** AdMob 等の依存を追加しない
- **サードパーティ分析 SDK を入れない。** Firebase Analytics, Amplitude 等すべて不可
- **IDFA / AdSupport.framework を参照しない。** `ASIdentifierManager` を binary に含めない
- **端末外へデータを送信しない。** ユーザー生成データはすべてローカル保存
- **アプリ外リンク・購入導線を作らない。**
  どうしても必要になったらペアレンタルゲートの内側に置く
- プライバシーラベルは「データを収集しません」を維持する

### やらないこと (Non-goals)

- アカウント登録・ログイン・クラウド同期
- ランキング・他ユーザーとの比較・SNS 共有
- 連続ログイン日数(ストリーク)とその喪失演出
- ライフ/ハート制、全問共通の制限時間
- ガチャ課金、消費型アイテム

---

## 2. ディレクトリ構成

```
wakuwaku-mapquiz/
├── CLAUDE.md
├── tools/
│   └── build_map_data.py          # GeoJSON → PrefectureShapes.json
├── ChizuHakase/
│   ├── App/
│   │   ├── ChizuHakaseApp.swift
│   │   └── AppState.swift          # @Observable ルート状態
│   ├── Models/
│   │   ├── Prefecture.swift        # 県マスタ(形状 + メタ)
│   │   ├── SpecialtyCard.swift     # カードマスタ
│   │   ├── CardTier.swift          # ふつう/シルバー/ゴールド/にじいろ
│   │   ├── Stage.swift             # 地方ステージ定義
│   │   ├── QuizMode.swift          # 出題モード(findOnMap / nameIt)
│   │   ├── GameRules.swift         # §5 の定数と純粋関数
│   │   ├── TextMode.swift          # こども/おとな 表記の語彙
│   │   └── SaveData.swift          # セーブ用 Codable
│   ├── Store/
│   │   ├── SaveStore.swift         # 読み書き + マイグレーション
│   │   └── MapDataLoader.swift     # JSON → [Prefecture]
│   ├── Features/
│   │   ├── Title/
│   │   ├── StageSelect/
│   │   ├── Quiz/                   # QuizViewModel, QuizView
│   │   ├── Result/
│   │   ├── MyMap/
│   │   └── CardBook/               # CardBookView, CardDetailView(拡大表示)
│   ├── Components/
│   │   ├── PrefectureMapView.swift # 地図描画 + タップ判定(再利用)
│   │   ├── PrefectureGeometry.swift# Path 生成 + ヒットテスト(純粋関数)
│   │   ├── ZoomPan.swift           # ピンチズームの計算。地図とカード拡大で共有
│   │   ├── CardFaceView.swift      # カードの絵柄。一覧と拡大で共有
│   │   ├── CardChipView.swift      # 一覧のマスにするだけの薄い包み
│   │   ├── BouncyButton.swift
│   │   ├── StickerStyle.swift      # シール表現の共通モディファイア
│   │   └── Palette.swift
│   ├── Services/
│   │   ├── AudioSession.swift      # 共有オーディオセッション設定
│   │   ├── SpeechService.swift     # 読み上げ
│   │   ├── VoiceInputService.swift # 音声入力
│   │   └── SoundService.swift      # 効果音
│   └── Resources/
│       ├── PrefectureShapes.json   # 生成物(コミットする)
│       ├── SpecialtyCards.json     # 生成物(コミットする)
│       └── Assets.xcassets
└── ChizuHakaseTests/
```

---

## 3. 地図データ

### 生成パイプライン

`Resources/PrefectureShapes.json` は生成物だが **リポジトリにコミットする**
(ビルド時に再生成しない)。更新が必要なときだけ以下を実行する。

```bash
cd tools
curl -sL -o japan.geojson \
  https://raw.githubusercontent.com/dataofjapan/land/master/japan.geojson
npx mapshaper japan.geojson \
  -simplify visvalingam 5% keep-shapes -clean \
  -o japan_simplified.geojson format=geojson
python3 build_map_data.py
mv PrefectureShapes.json ../ChizuHakase/Resources/
```

出典表記が必要。タイトル画面フッターに以下を残すこと。

> ちずデータ: Global Map Japan (国土地理院) をもとに簡略化

### JSON スキーマ

```jsonc
{
  "mapWidth": 1000.0,
  "mapHeight": 1066.7,
  "okinawaInset": [0.0, 832.5, 56.7, 915.9],  // 沖縄インセットの枠 [x0,y0,x1,y1]
  "prefectures": [
    {
      "code": 1,                      // 全国地方公共団体コードの都道府県部分 (1-47)
      "name": "北海道",
      "kana": "ほっかいどう",
      "bbox": [x0, y0, x1, y1],
      "centroid": [cx, cy],           // ラベル/エフェクトの表示位置
      "rings": [ [[x,y], ...], ... ]  // 先頭が外周、以降は穴
    }
  ]
}
```

座標系は **左上原点・y 下向き**(SwiftUI と同じ)、幅 1000 に正規化済み。
全 47 県 4,087 点、約 62 KB。

### 描画とタップ判定

```swift
// rings → Path
func makePath(rings: [[CGPoint]], transform: CGAffineTransform) -> Path {
    var path = Path()
    for ring in rings {
        guard let first = ring.first else { continue }
        path.move(to: first.applying(transform))
        for point in ring.dropFirst() { path.addLine(to: point.applying(transform)) }
        path.closeSubpath()
    }
    return path
}
```

- 穴(湖など)があるため塗りは **even-odd**。`path.fill(color, style: FillStyle(eoFill: true))`
- ヒットテストも `path.contains(point, eoFill: true)`
- 表示範囲はステージごとに可変。`bbox` の和集合に 4.5% + 6pt のパディングを足して
  `GeometryReader` のサイズへ **等比(aspect fit)** で収める。潰さないこと。
  当初は 9% + 8pt だったが、全国チャレンジは画面幅に合わせて縮尺が決まるため、
  余白はそのまま香川の大きさから引かれる
- **タップ許容**: 直接ヒットが無い場合、対象県の重心から一定距離内なら正解扱いにする。
  香川・大阪・沖縄など小さい県を子どもが指でタップできないと成立しない。
  スクリーン座標で 22pt 以内を目安に調整すること
- 沖縄は 1.6 倍に拡大し、**九州の南東の海**へ移動済み。`okinawaInset` の枠を
  破線で描き、「別枠である」ことを視覚的に示す。位置は
  `build_map_data.py` が空き海域を走査して決める(手置きしない)。
  満たすべき条件は 2 つあり、**どちらか一方だけでは壊れる**:
  - **本土の bbox の内側**に収めること。九州の西に置くと地図全体が 14% 横に広がり、
    全国チャレンジの全県がその分だけ小さく描かれる
  - **九州の近く**に置くこと。空いている北西の隅へ逃がすと今度は
    「きゅうしゅう・おきなわ」ステージが細長い帯になり、九州が豆粒になる

---

## 4. カードデータ

`Resources/SpecialtyCards.json`。47 県 × 3 枚 = **141 枚**。
生成手順は別途 Codex 用プロンプトを参照(このリポジトリでは生成物のみ管理)。

```jsonc
{
  "cards": [
    {
      "id": "01-1",              // "{県コード2桁}-{連番}"
      "prefectureCode": 1,
      "emoji": "🦀",
      "nameKana": "かに",
      "nameKanji": "蟹",
      "category": "food",        // food | landmark | nature | craft
      "description": "つめたい うみで そだつよ",
      "art": "card-01-1"         // 任意。カード絵がある札だけ持つ
    }
  ]
}
```

起動時に読み込み、`prefectureCode` で辞書化する。
`id` はセーブデータのキーになるので **一度公開したら変更しない**。

### カード絵

手描きのカード絵を `assets/meisanhin-cards/*-transparent.png` に置き、
以下で asset catalog へ書き出す。出力（imageset と `art` フィールド）はコミットする。

```bash
python3 tools/build_card_art.py
```

- 480px・192 色に落とす。原画は 1254px / 2.6MB あり、そのまま入れると
  オフライン前提のアプリに 50MB 乗る
- 絵とカードの対応は `tools/build_card_art.py` の `ART_FOR_CARD` に**明示的に書く**。
  ファイル名からの推測はしない。「乳製品」の絵を「蟹」の札に出すのは
  覚えようとしている子どもに嘘をつくことになる
- **取得済みの札は ★1 から絵を表示する。** 未取得の札だけ「❓」のまま。
  シルバー・ゴールド・レインボーの違いは、絵の有無ではなく地板・縁・星・
  箔の表現で伝える
- 絵が無い札は絵文字のまま。プレースホルダを出さない
- 日本編原画（`assets/meisanhin-cards/`）は git 管理外のローカル限定。
  世界編原画（`assets/world-cards/`）は消失防止のため **Git LFS** で管理する。
  どちらもアプリターゲットのソースではなく、出荷物には
  `ChizuHakase/Resources/Assets.xcassets/CardArt/` の 480px 版だけを入れる
- 絵に対応する札が無い場合、**公開前に限り**同じ県・同じカテゴリの札を
  絵の題材へ書き換えてよい。公開後は `id` がセーブキーなので不可（上記）

---

## 5. ゲームロジック

### ステージ

**7 ステージすべて最初から遊べる。** ロックも解放条件も置かない。
子どもがアプリを開いて日本のどこにも行けない状態を作らないための判断であり、
「進行度で解放する」「課金で解放する」提案は採用しない。

| # | 名前 | 県コード |
| --- | --- | --- |
| 0 | ほっかいどう・とうほく | 1–7 |
| 1 | かんとう | 8–14 |
| 2 | ちゅうぶ | 15–23 |
| 3 | きんき | 24–30 |
| 4 | ちゅうごく・しこく | 31–39 |
| 5 | きゅうしゅう・おきなわ | 40–47 |
| 6 | ぜんこく チャレンジ | 1–47 |

### 出題モード

**2 モードあり、ステージ選択の上で切り替える。** 出題の向きが違うだけで、
スコア・コンボ・星・カード・習熟レベルの規則は**両モード共通**。

| モード | 問い | 答え方 |
| --- | --- | --- |
| `findOnMap` | 「あいちけんは どこかな?」 | 地図をタップ |
| `nameIt` | 赤いわくで囲んだ県を見せる | 4 択（＋音声） |

- **習熟レベルは共有する。** 「形が分かる」と「名前が言える」は別の技能だが、
  マイマップを 2 面に分けると 5 歳児には把握できない
- **星とスコアはモードごとに分ける。** これは「回」の記録であり、別の回だから
- `nameIt` の誤答選択肢は**同じステージ内から**選ぶ。全国から選ぶと、
  画面の形だけで消去できてしまい問題にならない
- **自動読み上げは `findOnMap` だけ。** あちらは問題文が毎回違う県名を運ぶ。
  `nameIt` は毎問同じ文で答えは地図の上にあるため、自動で読むとループするノイズになる
- **`nameIt` に 🔊 ボタンは置かない。** 読める内容が存在しないため:
  正解県の名前は読めない(それが答えそのもの)し、4 択の読み上げは
  どれが正解かについて何も言わない(かつて読んでいて外した)。残るのは
  毎問同じ指示文だけで、ボタンの受益者がいない。このモードは選択肢を
  **読む**ことが本体なので、読めない子は「こえで こたえる」を使う
- 誤答した選択肢は**消さずに灰色で残す。** 消えると「もう試した」記憶ごと消える

### 1 問の流れ

1. 出題順はステージ内でシャッフル
2. 問題文は **ひらがな**を主・漢字を副で表示。`findOnMap` は 🔊 ボタンで読み上げ
3. **地方ステージは 1 県につき 2 回出題する**(全国チャレンジのみ 1 回)。
   同じ県を連続では出さない。星は「問題数」ではなく**県数**で判定する
4. **正解しても県の色は変えない。** 着色すると残りが減っていき、
   最後の問題は消去法で解けてしまう。ステージ内の県は最後までタップ可能
5. **正解**: 県が pop(1.0 → 1.22 → 0.95 → 1.0 / 0.55s)し、
   特産品絵文字が浮上、カードを 1 枚獲得、1.15s 後に次の問題へ。
   pop も絵文字も**過ぎ去る**演出であり、画面に残るものは問題のままにする
6. **不正解**: 誤タップした県が横に振動(0.45s)、コンボ 0、ミス記録
7. **3 回間違えた**時点で正解県の輪郭を赤で点滅(0.9s ループ)させる。
   当初 2 回だったが、考えている子から答えを取り上げる頻度が高すぎた
8. **1 回でも間違えたらカードは渡さない。** カードは「知っていた」印であり、
   つまずいた後に渡すと印にならない。点も習熟も従来どおり入る —
   渡さないだけで、**取り上げはしない**(§12)
9. 連続正解は**タップした位置に**「○ れんぞく!」を出す。
   2〜3 / 4〜6 / 7 以上で大きさ・色・✨ の数が上がる。1 連続では出さない

### スコア

- 1 回目で正解: `100 + (コンボ − 1) × 20`
- 2 回目以降で正解: `50`
- コンボは 1 回目正解でのみ加算、不正解でリセット

### 星評価(ステージ単位)

ミスした **県の数**(誤タップ回数ではない)で決める。

- 0 → ★★★
- `ceil(県数 / 4)` 以下 → ★★
- それ以外 → ★

**両辺とも県で数える。** 地方ステージは 1 県を 2 回訊くので、しきい値を問題数で
とるとそこだけ許容が倍になる。全国チャレンジは県数と問題数が等しく、
間違えても正しく見えてしまうので、地方ステージで確かめること
(`GameRules.stars(missedPrefectures:prefectureCount:)`)。

### 習熟レベル(全国マイマップ)

県ごとに 0–5。**1 回目で正解したときだけ +1**、上限 5(=**おぼえた**)。
**間違えても減らさない。** 子どもから積み上げを奪わないための設計判断であり、
「復習のために減衰させる」提案は採用しない。
当初は上限 3 だったが、まぐれの 3 回で「おぼえた」を名乗れてしまった。

| Lv | 表示 |
| --- | --- |
| 0 | グレー `#E7ECEF` |
| 1–2 | まなび緑 うすめ(`#A5D6A7` + 白 42%) |
| 3–4 | まなび緑 こいめ(`#A5D6A7` + 白 18%) |
| 5 | 金ベタ `#FFC53D` + ゆっくり明滅(**おぼえた**) |

- 色が運ぶのは「どれだけ知っているか」**だけ**。県ごとの色相は使わない
  (使うと凡例が嘘になる)。ランプと凡例は同じ関数(`MasteryStyle.fill`)から引く
- **5 レベルだが見た目は 4 状態。** 6 色の緑を子どもは見分けられないし、
  6 マスの凡例は凡例ではない。増えたレベルは登りを遅くするためのもの
- タイトルの「おぼえた けん」は **Lv5 到達数**で数える。1 回の一発正解を
  「おぼえた」と呼ぶと、地図が緑のうちにバーが満タンになる
- **最上位の状態名は「おぼえた」。**「キラキラ」と呼んでいたが、県の金色と
  カードの「キラカード」で同じ語が 2 つの物を指し、はしごの語感
  (まだ → すこし おぼえた → おぼえてきた → おぼえた)も切れていた。
  金色の明滅はそのまま — 光り方が変わるのではなく、光り方が名前でなくなるだけ
- Lv5 到達時は結果画面で「✨ おぼえた けん!」として個別に祝う

### カード獲得

正解するたびに、その県のカードを 1 枚獲得する。
**同じ札をもう 1 枚引くたびに星が 1 つ増える**(上限 10)。

```
未所持のカードがあれば、その中からランダムに 1 枚 (★1)
そうでなければ、★10 未満の札からランダムに 1 枚 → ★ +1
全部 ★10 なら「もっている カードだね」(増えない)
```

| 段位 | 条件 |
| --- | --- |
| ふつう | ★1–4 |
| **シルバーカード** | ★5–9 |
| **ゴールドカード** | ★10 |
| **にじいろカード** | 金になった後の県のれんぞく正解 5(下記) |

- 未所持を優先することでコレクションが早く埋まり、
  埋まった後は星を伸ばす動機が残る
- **金は ★10。** 当初は ★15 だったが、抽選が 3 枚に散るぶん初金は
  同じステージをミスなしで約 18 周した先にあり、誰も到達しない最上段は
  目標ではなく天井だった。★10 でも約 11 周で、金は珍しいままでいる
- **★10 に達していない札から引く。** 全部から引くと、埋まりかけの県では
  当たりが ★10 の札に吸われ、正解しても何も起きない回が増える
- **カード絵はシルバー以上で出す**(§4)。ゴールド(同じ札 10 枚ぶん)まで
  隠すと大半の絵が誰にも見られない
- 星は「持っている枚数」であって強さではない。**減らさない**
- カード面の星は「★8」のように**星 1 個+数字**で描く。10 個は並べられない

### にじいろカード(虹)

- 県ごとに「れんぞく正解数」を持つ。**その県が問題として出たとき**に
  一発正解で +1、つまずいたら(2 回目以降で正解した場合も)0 に戻る。
  **他の県の問題で誤タップしても、その県のれんぞくは切れない**(訊かれていない)
- セッションをまたいで数える。地方ステージは 1 県 2 回出題なので 1 プレイ最大 +2
- **金(★10)が先、れんぞくが後。** 札が金になった瞬間にもカウンタは 0 に戻る
  (金を完成させた答えは数えない)。そこかられんぞくが **5** に達した時点で、
  **その時点で金だった札**が虹になる。金より前に積んだれんぞくは持ち越さない —
  虹は「完成した札を持ったまま走り切った」勲章だから。当初は順不同のれんぞく 15
  だったが、金と同時に虹まで付いてくる回があり、二つの到達が別の出来事にならなかった。
  7 から 5 へ縮めたのは金の ★15 → ★10 と同じ判断 — 地方ステージは 1 プレイ
  最大 +2 なので、5 なら金から 3 周で届く
- 判定はれんぞくが 5 に**触れた瞬間**に行う(`GameRules.nextStreak` が出題結果と
  抽選列を突き合わせて歩く)。最終値だけを見ると、同じステージの 2 問目の
  つまずきが 1 問目で届いた虹を握りつぶす
- **一度虹になったら戻らない。** れんぞくが切れても(金昇格で 0 に戻るときも)
  カウンタが黙って戻るだけで、「きれた」とは言わない・見せない(§12)。
  星も段位も動かない
- 進捗は「あと ◯かいで シルバー/ゴールド」「あと ◯れんぞくで にじいろ」として
  **カード拡大表示**と**結果画面の獲得カード欄**に出す(`GameRules.nextGoal`)。
  未所持の札には出さない — 最初の 1 枚は空きマスがすでに語っている
- 文言の下に**小さなプログレスバー**(`NextGoalBar`)を添える。割合は同じ
  `NextGoal` から導く(`fraction`)ので文言と食い違えない。**段ごとに空から
  埋まる**(銀への進みは金の段に持ち越さない — 持ち越すとバーはゲームの大半で
  ほぼ満タンのまま意味を失う)。塗りは目指す先の色: 銀・金・虹のグラデーション。
  数字は文言が言うので、バーは VoiceOver から隠す
- 虹になった回は**結果画面の先頭で「🌈 にじいろに なった カード!」として祝う**。
  名前ではなく**カードそのもの**を出す(タップで拡大できる)。虹箔が報酬なので、
  文字だけの札では何も渡していない
- **この祝いは獲得カード欄では代用できない。** れんぞく 5 に届いた時点で
  その県の(その時点で金の)札が**まとめて**虹になり、そのステージで
  引いていない札も混ざる。
  `SaveStore.applyStageResult` が今回掛かった札を `StageGains` で返すのはこのため
- **祝うのは掛かった回だけ。** すでに虹の札を毎回並べると、いちばん珍しいものが
  背景になる

---

## 6. セーブデータ

`FileManager` の Application Support に JSON で保存。SwiftData は使わない
(構造が単純で、マイグレーションを自前で握れるほうが安全)。

```swift
struct SaveData: Codable {
    var version: Int = 6
    var mastery: [Int: Int] = [:]        // 県コード → 0...5(モード共有)
    var cards: [String: Int] = [:]       // カードID → 星の数 (0...10)
    var rainbow: Set<String> = []        // 虹に到達したカードID
    var streaks: [Int: Int] = [:]        // 県コード → れんぞく正解数
    // モード → ステージ# → ベスト記録
    var records: [String: [Int: StageRecord]] = [:]
    var settings: Settings = .init()
}

struct StageRecord: Codable { var stars: Int; var score: Int }
struct Settings: Codable {
    var soundEnabled = true
    var speechEnabled = true
    var voiceInputEnabled = false
}
```

- 書き込みは **ステージ終了時のみ**。1 問ごとに書かない
- 保存は atomic write。読み込み失敗時は初期値にフォールバックしてクラッシュさせない
- `rainbow` は星とれんぞくから**導出できない**(れんぞくは後で切れる)ので、
  到達した事実として焼き付ける
- `version` を見て将来のマイグレーションを分岐する。
  **v1 → v2**(記録のモード別分割)、**v2 → v3**(カードを 5 段階の星に)、
  **v3 → v4**(星の尺度を 15 段階に)、**v4 → v5**(習熟を 5 回制に)、
  **v5 → v6**(星の上限を 10 に)は `SaveData.init(from:)` の中で行う。
  v3 では **旧「キラ」(所持数 2)を旧スケールの上限に読み替える**(§12:
  すでに手に入れたキラを取り上げない)。
  v4 では旧★を**段位の位置で**現行ラダーへ直接写す: 1→1, 2→2,
  3→**5**(銀の下限), 4→**7**(銀区間の中間), 5→**10**(金のまま)。
  子どもが見ているのは段位であり、段位を落とさないことが基準。
  **虹は付与しない** — れんぞくの勲章で、まだ誰も歩いていない。
  v5 では旧習熟を**見た目の状態を保って**写す: 1→**2**(うすい緑のまま、
  こい緑まであと 1)、2→**4**(こい緑のまま、金まであと 1)、3→**5**
  (キラキラはキラキラのまま)。地図の色も「つぎまであと何回」も悪化させない。
  v6 では 10 を超える星を 10 に丸める。段位は上がることしかない
  (★11–14 の銀は金に昇格し、金は金のまま)— 丸めで下がるのは数字だけで、
  その数字も新しい最上段に立っている。
  デコードが常に現行形を返すようにしておけば、
  中途半端な状態が書き戻される隙間が無くなる。
  未来のバージョンのファイルは番号を下げない(古いビルドで開いても壊さない)
- 記録の全削除はマイマップ画面から。必ず 2 段階確認を挟む

---

## 7. 音声

### 読み上げ (`SpeechService`)

`AVSpeechSynthesizer`。オンデバイスで完結し課金対象外。

- **声は端末に入っている最良の日本語音声を選ぶ。**
  `AVSpeechSynthesisVoice(language: "ja-JP")` は使わない — 返るのは既定音声で、
  設定を触ったことのない端末では compact の Kyoko(数 MB の継ぎ接ぎサンプル)になる。
  enhanced / premium はただの無料ダウンロードで、既に入っている端末が多い。
  インストール済みを品質順に並べ、同品質は identifier で決めて
  起動ごとに声が変わらないようにする。ノベルティ音声(ジョーク)と
  パーソナルボイス(録音した本人のもの)は除外。
  日本語音声が 1 つも無ければ既定へフォールバック(compact でも読まないよりまし)
- 選定ルールは `VoiceOption` 上の純粋関数として切り出す。
  実行機に何がダウンロード済みかへテストが依存しないため
- `rate = 0.42`。既定より遅め。子どもは既定速度だと聞き取れない
- compact 音声だけ `pitchMultiplier = 1.05` で薄さを和らげる。
  enhanced / premium は人の録音であり、ピッチを動かすと輪のかかった響きになる
- 複数フレーズは 1 つの文字列に連結せず、utterance を分けて 0.3s の間を置く。
  つなげると「どこかな?なまえを えらんでね」がひと息で届いてしまう
- オーディオセッションは `AudioSession` の 1 箇所で設定する。
  ambient + mixWithOthers(おもちゃなので他の音と共存し、消音スイッチに従う)。
  音声入力が奪ったら必ず戻す — 最後に触った者が読み上げの可否を決めてしまう

読み上げは **無料機能**。課金や広告の後ろに置かない。ひらがなが読めない層にとって
これが無いとアプリが成立しないため。

### 音声入力 (`VoiceInputService`)

`SFSpeechRecognizer` を **オンデバイス限定**で使う。

```swift
request.requiresOnDeviceRecognition = true   // 必須。音声を外部送信しない
request.contextualStrings = allPrefectureKanaNames  // 47 語の固定語彙
```

- **「なまえを あてる」専用の機能。**「ちずで さがす」は問題文が県名を
  運ぶので、声で名前を言っても答えにならない — マイクボタン自体を出さない
- **マイクボタンはモードのトグル。** 一度押したら再度押すまでオンのまま、
  問題をまたいで続く(1 問ごとに押し直させると片手がボタンに取られる)。
  ただし認識セッションは 1 問ごとに張り直す — 途中結果の transcript は
  伸び続けるため、前問の発話が次問の照合を汚す
- **読み上げ中はマイクを閉じる。** 開けたままだとアプリが自分の声を聞いて
  誤作動する。読み上げ終了(`SpeechService.isSpeaking`)を待って再開すること
- 判定は正規化した読みの一致 + 表記ゆれ吸収
  (「とうきょう」「とうきょうと」「東京」をすべて可)
- マイク権限が拒否された場合は静かにタップモードへフォールバック。
  権限を再要求してループさせない
- **オンデバイス認識が使えない端末では機能自体を隠す**

---

## 8. 課金

**課金は無い。** StoreKit を組み込まない。

全 7 ステージ・全 141 枚のカード・読み上げ・音声入力を、
インストール直後から誰の許可も要らずに使える状態にしておく。
子どもが自分で開いて自分で遊びきれることを、収益より優先する。

したがって以下も存在しない。復活させるときは §1 の制約に戻って設計しなおすこと。

- 購入・復元ボタン、価格表示、購入導線
- ペアレンタルゲート(守る対象が無いため)
- ステージのロック表示・「保護者の方と一緒に」といった文言

---

## 9. デザイン

### トークン

```swift
enum Palette {
    static let background = Color(hex: 0xFFF7E8)
    static let ink        = Color(hex: 0x3D3A4B)
    static let orange     = Color(hex: 0xFF9F1C)   // 主 CTA
    static let teal       = Color(hex: 0x2EC4B6)   // 副次アクション
    static let red        = Color(hex: 0xFF5D5D)   // 正解演出・ヒント
    static let gold       = Color(hex: 0xFFC53D)   // キラキラ / キラカード
    static let sea        = [Color(hex: 0xCDEFFB), Color(hex: 0xA9DFF2)]
}
```

県の塗り分けは 8 色パレットを `県コード % 8` で循環させる。
`#FF8A80 #FFCC80 #FFF176 #A5D6A7 #81D4FA #CE93D8 #F48FB1 #80CBC4`

### 書体・形状

- 丸ゴシック(ヒラギノ丸ゴ ProN)。UI 文言は基本 Bold 以上
- ボタンは角丸 999(ピル型)+ 下方向のソリッドシャドウ。押下で 3–4pt 沈む
- パネルは角丸 18–24。カードは実物の比率(0.7)と角丸 9–12
- **カードの絵柄は 1 つ(`CardFaceView`)。** ずかん・マイマップ・結果画面の
  マスも、タップして開いた 1 枚も同じ絵柄を描く。変えていいのは密度
  (文字サイズと、小さな文字を出すかどうか)だけ。一覧用に別のデザインを
  作らない — かつてマスはシール、開くとカードで、集めている物が何なのか
  一覧からは分からなかった
- ★1–4 は寒色のマット台紙、★5 は銀の箔、★10 は金の箔、虹はパステルの虹箔。
  **ふつうの札とその上を「光り方の差」にしない。** 弱く光るという差は、
  2 枚並べないと子どもには見えない。銀・金・虹は色相でも分かれている。
  絵の上のシーンは金が虹色・銀と虹は白 — 虹箔の上に虹のシーンはノイズにしかならない
- 拡大したカードは印刷された長方形ではなく**手に取れる物**として振る舞う。
  ドラッグで最大 26° まで傾き、傾くと紙芯の厚みが縁に見える。
  ピンチズームは地図と同じ `ZoomPan` を使う(指の間の点を保ち、
  フレームの外へは逃げない)。傾けるジェスチャは等倍のときだけ —
  ズーム中のドラッグはパンで、同じ 1 本指を取り合わない。
  「とじる」は画面下端に置く。見ている物の縁にボタンを貼り付けない

### 画面の向き

- **iPhone は縦固定。** 地図の縦横比とタップ到達性は縦持ちに合わせてある
- **iPad は 4 方向すべて対応する。** 好みではなく App Store の検証条件
  (iPad マルチタスキング)。逃げ道だった `UIRequiresFullScreen` は
  iPadOS 26 で非推奨になり「近く無視する」と予告済みなので使わない
- 横向き用の 2 つ目のレイアウトは**作らない**。各画面は 1 カラムのまま
  `pageColumn()`(最大 700pt・中央寄せ)で幅を保ち、余りはアルバム台紙の
  余白にする。端末を回しても**何かの大きさが変わらない**ことを優先する。
  新しい画面を足すときはスクロールカラムに `pageColumn()` を付けること

### アクセシビリティ

- `@Environment(\.accessibilityReduceMotion)` を尊重。true のときは
  pop / 振動 / 紙吹雪 / 明滅をすべて無効化し、状態変化は色のみで表現する
- 各県の `Path` に `.accessibilityLabel(県名)` を付ける
- タップターゲットは実質 44pt 以上を確保(§3 のタップ許容で担保)
- Dynamic Type は問題文と結果画面で追従させる。地図内ラベルは固定でよい

---

## 10. 実装順序

各フェーズが動く状態で区切ること。まとめて書かない。

1. **データ層** — `MapDataLoader` + `SaveStore` + ユニットテスト。
   47 県すべての `Path` が空でないこと、`contains` が重心で true を返すことを検証
2. **地図コンポーネント** — `PrefectureMapView` 単体。
   Preview でステージ切り替えとタップ判定を確認
3. **クイズ 1 ステージ** — 出題・正誤・スコア・コンボ・ヒント
4. **結果 + セーブ** — 星評価、習熟レベル、カード獲得、永続化
5. **マイマップ / ずかん**
6. **読み上げ・効果音**
7. **音声入力モード**
8. 仕上げ(Reduce Motion、アイコン、スクリーンショット)

---

## 11. コーディング規約

- 状態は `@Observable`(Observation フレームワーク)。`ObservableObject` は使わない
- View に業務ロジックを書かない。判定・スコア計算は ViewModel か純関数へ
- スコア・星・習熟・カード抽選は **純粋関数**として切り出し、テストを書く
- `print` デバッグを残さない。必要なら `OSLog`
- 強制アンラップ `!` と `try!` を使わない。データ読み込み失敗は握って初期状態へ
- マジックナンバーは §5 の定数として `GameRules` にまとめる
- コメントは「なぜそうしたか」を書く。「何をしているか」はコードで示す

## 12. 迷ったら

- **子どもが不安になる方向には倒さない。** 失うもの・急かすもの・叱るものは作らない
- 仕様に無い機能を推測で追加しない。特に §1 の制約に触れる依存追加は必ず確認する
- 文言に迷ったら短く・ひらがなで・肯定形で
